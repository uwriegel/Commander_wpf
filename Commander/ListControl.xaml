<UserControl x:Class="Commander.ListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Commander"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <UserControl.Resources>
        <local:FileIconConverter x:Key="FileIconConverter" />
        <local:FileSizeConverter x:Key="FileSizeConverter" />
        <local:ColumnsSizes x:Key="ColumnsSizes"/>
    </UserControl.Resources>
    <Grid Grid.IsSharedSizeScope="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition SharedSizeGroup="A"/>
                <ColumnDefinition SharedSizeGroup="B"/>
                <ColumnDefinition SharedSizeGroup="C"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Name"/>
            <TextBlock Text="Größe" Grid.Column="1"/>
            <TextBlock Text="Datum" Grid.Column="2"/>
        </Grid>
        <ListBox Name="List" BorderThickness="0" 
                 Grid.Row="1"
                 VirtualizingPanel.IsVirtualizing="true" 
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Canvas d:DataContext="{d:DesignData /SampleData/FileItemSampleData.xaml}" Height="16" >
                        <Border Width="16" Height="16" Background="{Binding Name, Converter={StaticResource FileIconConverter}}" HorizontalAlignment="Left"></Border>
                        <TextBlock Text="{Binding Name}" Canvas.Left="20" />
                        <TextBlock Text="{Binding Size, Converter={StaticResource FileSizeConverter}}" Canvas.Left="{Binding ColumnsSizes.Size2, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:ListControl}}}"  />
                        <TextBlock Text="{Binding Date}" Canvas.Left="{Binding ColumnsSizes.Size3, 
                            RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:ListControl}}}" />
                    </Canvas>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
